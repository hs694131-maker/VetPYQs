RAJUVAS AI — START HERE

I connected the AI button to your existing RAJUVAS Question Bank.

IMPORTANT:
- I cannot create an external hosting account or enter your private API key for you.
- Your API key must remain secret.
- The supplied backend is ready to deploy.

LOCAL:
1. Install Node.js LTS.
2. Open rajuvas-ai-backend.
3. npm install
4. Copy .env.example to .env
5. Put your key after OPENAI_API_KEY=
6. npm start
7. Backend: http://localhost:3000
8. Put that URL into the website's AI Backend URL field.

PUBLIC:
Deploy the rajuvas-ai-backend folder to a Node-compatible host and set OPENAI_API_KEY in the host's environment variables. Then use the resulting HTTPS URL in the website.
